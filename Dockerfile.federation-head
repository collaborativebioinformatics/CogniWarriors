FROM python:3.9-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY center.py .

EXPOSE 8080

CMD ["python3", "center.py", "--rounds", "5", "--epochs", "10"]
